Jules-friendly test instructions

Install:
  python -m pip install -U pip
  pip install -r requirements.txt
  pip install "pytest==8.3.*" "pytest-timeout==2.3.*"

Run:
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q
  # or
  bash run_tests.sh
